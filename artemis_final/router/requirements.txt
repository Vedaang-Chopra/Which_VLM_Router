# Artemis Router Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
torch>=2.0.0
transformers>=4.30.0
sqlalchemy>=2.0.0
pandas>=2.0.0
pillow>=10.0.0
pyyaml>=6.0
numpy>=1.24.0

# Optional dependencies (recommended)
wandb>=0.15.0           # For Weights & Biases logging
requests>=2.31.0        # For HTTP load balancer
psycopg2-binary>=2.9.0  # For PostgreSQL (alternative: psycopg2)

# Development/notebook dependencies
jupyter>=1.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0     # For confusion matrix

# GPU support (uncomment if using CUDA)
# Install the appropriate version for your CUDA version from:
# https://pytorch.org/get-started/locally/
# torch>=2.0.0+cu118
